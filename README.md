# Ravindra Iron - Homepage

## Setup
1. Install dependencies:
```npm install```

2. Start dev server:
```npm run dev```

3. Open http://localhost:3000

## Notes
- Add actual images to /public/images (hero.jpg, facility.jpg, production.jpg).
- Analytics (GA4) and admin dashboard to be added after design sign-off.
- Contact endpoint is at `/api/contact` (currently returns success JSON).